flask>=2.3.0
requests>=2.31.0
psycopg2-binary>=2.9.9
python-dotenv>=1.0.0
openai>=1.3.0
markdown>=3.5.0

# Local embedding model (optional - for free offline vector search)
# CPU-only installation (recommended, ~200MB):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install sentence-transformers
# GPU installation (requires NVIDIA GPU, ~3GB+):
#   pip install torch
#   pip install sentence-transformers
#sentence-transformers>=2.2.0
#torch>=2.0.0
pgvector>=0.2.0